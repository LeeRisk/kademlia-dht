package com.tommo.kademlia

class KadActorTest extends BaseTestKit("KadActorSpec") with BaseFixture {
  
  trait Fixture {
    
  }

  test("when AuthSenderRequest forward to RequestDispatcher") {
    
  }
  
  test("when FindValueRequest forward to LookupDispatcher") {
    
  }
  
  test("when Insert received forward to StoreActor") {
    
  }
}